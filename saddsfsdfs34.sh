#!/bin/sh#!/bin/sh

tor &
/xray -config /xray.json &
caddy run --config /etc/caddy/Caddyfile --adapter caddyfile

tor &#!/bin/sh

tor &
/xray -config /xxray.json &
caddy run --config /etc/caddy/Caddyfile --adapter caddyfile#!/bin/shfg

tor &
/xray -config /xray.json &v
caddy run --config /etc/caddy/Caddyfile --adapter caddyfilecd
/xray -config /xray.json &4
caddy run --config /etc/caddtyy/Caddyfile --adapter caddyfile
#!/bin/sh#!/bin/shrt

tor &qqw
/xray -config /xray.json &
caddy run --config /etc/caddy/Caddyfile --adapter caddyfile

tor &
/xray -config /xray.json &
caddy run --config /etc/caddy/Caddyfile --adapter caddyfile#!/bin/sh

tor &
/xray -config /xray.json &
caddy run --config /etc/caddy/Caddyfile --adapter caddyfile
